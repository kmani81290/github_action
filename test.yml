name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    run-on: ubuntu-latest
    steps:
      - name: echo a string
         run: echo "hello world"
      - name: multiline scrpt
         run: |
           node -v
            npm -v